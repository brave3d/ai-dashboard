MODELS = [  
    {
        "name": "FLUX1.1 [pro] ultra",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-pro-11-ultra.webp",
        "summary": "FLUX1.1 [pro] ultra is the newest version of FLUX1.1 [pro], maintaining professional-grade image quality while delivering up to 2K resolution with improved photo realism.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-pro/v1.1-ultra"
    },
    {
        "name": "FLUX1.1 [pro] ultra Fine-tuned",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-pro-11-ultra.webp",
        "summary": "FLUX1.1 [pro] ultra fine-tuned is the newest version of FLUX1.1 [pro] with a fine-tuned LoRA, maintaining professional-grade image quality while delivering up to 2K resolution with improved photo realism.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-pro/v1.1-ultra-finetuned"
    },
    {
        "name": "FLUX.1 [dev]",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-dev-thumb.webp",
        "summary": "FLUX.1 [dev] is a 12 billion parameter flow transformer that generates high-quality images from text. It is suitable for personal and commercial use.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux/dev"
    },
    {
        "name": "FLUX.1 [dev] with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux_lora.jpg",
        "summary": "Super fast endpoint for the FLUX.1 [dev] model with LoRA support, enabling rapid and high-quality image generation using pre-trained LoRA adaptations for personalization, specific styles, brand identities, and product-specific outputs.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-lora"
    },
    {
        "name": "FLUX.1 [dev] Inpainting with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux_lora.jpg",
        "summary": "Super fast endpoint for the FLUX.1 [dev] inpainting model with LoRA support, enabling rapid and high-quality image inpainting using pre-trained LoRA adaptations for personalization, specific styles, brand identities, and product-specific outputs.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-lora/inpainting"
    },
    {
        "name": "FLUX.1 [schnell]",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-schnell-thumb.webp",
        "summary": "FLUX.1 [schnell] is a 12 billion parameter flow transformer that generates high-quality images from text in 1 to 4 steps, suitable for personal and commercial use.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux/schnell"
    },
    {
        "name": "FLUX.1 Subject",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-subject.webp",
        "summary": "Super fast endpoint for the FLUX.1 [schnell] model with subject input capabilities, enabling rapid and high-quality image generation for personalization, specific styles, brand identities, and product-specific outputs.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-subject"
    },
    {
        "name": "FLUX.1 [schnell] Redux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-schnell-thumb.webp",
        "summary": "FLUX.1 [schnell] Redux is a high-performance endpoint for the FLUX.1 [schnell] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux/schnell/redux"
    },
    {
        "name": "FLUX.1 [dev] Redux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-dev-thumb.webp",
        "summary": "FLUX.1 [dev] Redux is a high-performance endpoint for the FLUX.1 [dev] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux/dev/redux"
    },
    {
        "name": "FLUX.1 [pro] Redux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "FLUX.1 [pro] Redux is a high-performance endpoint for the FLUX.1 [pro] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/redux"
    },
    {
        "name": "FLUX1.1 [pro] Redux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/turbo_thumbnail.jpg",
        "summary": "FLUX1.1 [pro] Redux is a high-performance endpoint for the FLUX1.1 [pro] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1.1/redux"
    },
    {
        "name": "FLUX1.1 [pro] ultra Redux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-pro-11-ultra.webp",
        "summary": "FLUX1.1 [pro] ultra Redux is a high-performance endpoint for the FLUX1.1 [pro] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1.1-ultra/redux"
    },
    {
        "name": "FLUX.1 [pro] Fill",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "FLUX.1 [pro] Fill is a high-performance endpoint for the FLUX.1 [pro] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/fill"
    },
    {
        "name": "FLUX.1 [pro] Fill Fine-tuned",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "FLUX.1 [pro] Fill Fine-tuned is a high-performance endpoint for the FLUX.1 [pro] model with a fine-tuned LoRA that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/fill-finetuned"
    },
    {
        "name": "FLUX.1 [pro] Canny",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "Utilize Flux.1 [pro] Controlnet to generate high-quality images with precise control over composition, style, and structure through advanced edge detection and guidance mechanisms.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/canny"
    },
    {
        "name": "FLUX.1 [pro] Canny Fine-tuned",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "Utilize Flux.1 [pro] Controlnet with a fine-tuned LoRA to generate high-quality images with precise control over composition, style, and structure through advanced edge detection and guidance mechanisms.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/canny-finetuned"
    },
    {
        "name": "FLUX.1 [pro] Depth",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "Generate high-quality images from depth maps using Flux.1 [pro] depth estimation model. The model produces accurate depth representations for scene understanding and 3D visualization.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/depth"
    },
    {
        "name": "FLUX.1 [pro] Depth Fine-tuned",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/fluxpro.jpg",
        "summary": "Generate high-quality images from depth maps using Flux.1 [pro] depth estimation model with a fine-tuned LoRA. The model produces accurate depth representations for scene understanding and 3D visualization.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pro/v1/depth-finetuned"
    },
    {
        "name": "FLUX.1 [dev] Canny with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux_lora.jpg",
        "summary": "Utilize Flux.1 [dev] Controlnet to generate high-quality images with precise control over composition, style, and structure through advanced edge detection and guidance mechanisms.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-lora-canny"
    },
    {
        "name": "FLUX.1 [dev] Depth with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux_lora.jpg",
        "summary": "Generate high-quality images from depth maps using Flux.1 [dev] depth estimation model. The model produces accurate depth representations for scene understanding and 3D visualization.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-lora-depth"
    },
    {
        "name": "FLUX1.1 [pro]",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/turbo_thumbnail.jpg",
        "summary": "FLUX1.1 [pro] is an enhanced version of FLUX.1 [pro], improved image generation capabilities, delivering superior composition, detail, and artistic fidelity compared to its predecessor.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-pro/v1.1"
    },
    {
        "name": "FLUX.1 [pro]",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-pro-thumb.webp",
        "summary": "FLUX.1 [pro] new is an accelerated version of FLUX.1 [pro], maintaining professional-grade image quality while delivering significantly faster generation speeds.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-pro/new"
    },
    {
        "name": "FLUX.1 [dev] Fill with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux_lora.jpg",
        "summary": "FLUX.1 [dev] Fill is a high-performance endpoint for the FLUX.1 [pro] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-lora-fill"
    },
    {
        "name": "FLUX.1 [dev] with LoRAs",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/h2glDvCl5RvtgjNx_-5qY.jpeg",
        "summary": "FLUX LoRA Image-to-Image is a high-performance endpoint that transforms existing images using FLUX models, leveraging LoRA adaptations to enable rapid and precise image style transfer, modifications, and artistic variations.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-lora/image-to-image"
    },
    {
        "name": "FLUX.1 [dev] with Controlnets and Loras",
        "thumbnail": "https://storage.googleapis.com/falserverless/flux-lora/flux_general.png",
        "summary": "A versatile endpoint for the FLUX.1 [dev] model that supports multiple AI extensions including LoRA, ControlNet conditioning, and IP-Adapter integration, enabling comprehensive control over image generation through various guidance methods.",
        "category": "text-to-image",
        "model_path": "fal-ai/flux-general"
    },
    {
        "name": "FLUX.1 [dev] with Controlnets and Loras",
        "thumbnail": "https://storage.googleapis.com/falserverless/flux-lora/flux_general.png",
        "summary": "FLUX General Inpainting is a versatile endpoint that enables precise image editing and completion, supporting multiple AI extensions including LoRA, ControlNet, and IP-Adapter for enhanced control over inpainting results and sophisticated image modifications.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-general/inpainting"
    },
    {
        "name": "FLUX.1 [dev] with Controlnets and Loras",
        "thumbnail": "https://storage.googleapis.com/falserverless/flux-lora/flux_general.png",
        "summary": "FLUX General Image-to-Image is a versatile endpoint that transforms existing images with support for LoRA, ControlNet, and IP-Adapter extensions, enabling precise control over style transfer, modifications, and artistic variations through multiple guidance methods.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-general/image-to-image"
    },
    {
        "name": "FLUX.1 [dev] with Controlnets and Loras",
        "thumbnail": "https://storage.googleapis.com/falserverless/flux-lora/flux_general.png",
        "summary": "A specialized FLUX endpoint combining differential diffusion control with LoRA, ControlNet, and IP-Adapter support, enabling precise, region-specific image transformations through customizable change maps.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-general/differential-diffusion"
    },
    {
        "name": "FLUX.1 [dev] with Controlnets and Loras",
        "thumbnail": "https://storage.googleapis.com/falserverless/flux-lora/flux_general.png",
        "summary": "A general purpose endpoint for the FLUX.1 [dev] model, implementing the RF-Inversion pipeline. This can be used to edit a reference image based on a prompt.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-general/rf-inversion"
    },
    {
        "name": "FLUX.1 [dev] Differential Diffusion",
        "thumbnail": "https://fal.media/files/tiger/e-8Q3gR-WkrPM9-p4aicX.webp",
        "summary": "FLUX.1 Differential Diffusion is a rapid endpoint that enables swift, granular control over image transformations through change maps, delivering fast and precise region-specific modifications while maintaining FLUX.1 [dev]'s high-quality output.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-differential-diffusion"
    },
    {
        "name": "FLUX.1 [dev] Image to Image",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/yD32jWhfafC-vVBynYxey.png", // Re-using a relevant thumbnail
        "summary": "FLUX.1 Image-to-Image is a high-performance endpoint for the FLUX.1 [dev] model that enables rapid transformation of existing images, delivering high-quality style transfers and image modifications with the core FLUX capabilities.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux/dev/image-to-image"
    },
    {
        "name": "Train Flux LoRA",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-lora-fast-training.webp",
        "summary": "Train styles, people and other subjects at blazing speeds.",
        "category": "training",
        "model_path": "fal-ai/flux-lora-fast-training"
    },
    {
        "name": "Train Flux LoRAs For Portraits",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/portrait_thumbnail.jpg",
        "summary": "FLUX LoRA training optimized for portrait generation, with bright highlights, excellent prompt following and highly detailed results.",
        "category": "training",
        "model_path": "fal-ai/flux-lora-portrait-trainer"
    },
    {
        "name": "Train Flux LoRAs For Pro Models",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/idk_thumb.png",
        "summary": "FLUX LoRA for Pro endpoints.",
        "category": "training",
        "model_path": "fal-ai/flux-pro-trainer"
    },
    {
        "name": "PuLID Flux",
        "thumbnail": "https://storage.googleapis.com/falserverless/gallery/flux-pulid.webp",
        "summary": "An endpoint for personalized image generation using Flux as per given description.",
        "category": "image-to-image",
        "model_path": "fal-ai/flux-pulid"
    },
    {  
      "name": "AuraFlow",  
      "thumbnail": "https://fal.ai/static/media/aura-flow.7039467a.png",  
      "summary": "Fully open flow based text to image model",  
      "category": "text-to-image",  
      "model_path": "fal-ai/aura-flow"  
    },  
    {  
      "name": "FLUX.1 [dev]",  
      "thumbnail": "https://fal.ai/static/media/flux.7039467a.png",  
      "summary": "FLUX.1, a 12B parameters text-to-image model with outstanding aesthetics.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/flux/dev"  
    },  
    {  
      "name": "FLUX Realism LoRA",  
      "thumbnail": "https://fal.ai/static/media/flux-realism.7039467a.png",  
      "summary": "FLUX Realism LoRA is a cutting edge model for generating realistic images with the SOTA Flux Model.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/flux-realism"  
    },  
    {  
      "name": "FLUX.1 [schnell]",  
      "thumbnail": "https://fal.ai/static/media/flux.7039467a.png",  
      "summary": "A distilled version of FLUX.1 that operates up to 10 times faster.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/flux/schnell"  
    },  
    {  
      "name": "FLUX.1 [pro]",  
      "thumbnail": "https://fal.ai/static/media/flux.7039467a.png",  
      "summary": "The pro version of FLUX.1, served in partnership with BFL",  
      "category": "text-to-image",  
      "model_path": "fal-ai/flux-pro"  
    },  
    {  
      "name": "FLUX.1 [dev] with LoRAs",  
      "thumbnail": "https://fal.ai/static/media/flux.7039467a.png",  
      "summary": "A general purpose endpoint for the FLUX.1 [dev] model, which can be used with a variety of extensions including any LoRA support.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/flux-general"  
    },  
    {  
      "name": "Stable Diffusion V3",  
      "thumbnail": "https://fal.ai/static/media/stable-diffusion-v3-medium.7039467a.png",  
      "summary": "Run SD3 at the speed of light",  
      "category": "text-to-image",  
      "model_path": "fal-ai/stable-diffusion-v3-medium"  
    },  
    {  
      "name": "Stable Diffusion XL",  
      "thumbnail": "https://fal.ai/static/media/fast-sdxl.7039467a.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fast-sdxl"  
    },  
    {  
      "name": "Stable Diffusion with LoRAs",  
      "thumbnail": "https://fal.ai/static/media/lora.41923137.png",  
      "summary": "Run Any Stable Diffusion model with customizable LoRA weights.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/lora"  
    },  
    {  
      "name": "AuraSR",  
      "thumbnail": "https://fal.ai/static/media/aura-sr.7039467a.png",  
      "summary": "Upscale your images with AuraSR.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/aura-sr"  
    },  
    {  
      "name": "Stable Cascade",  
      "thumbnail": "https://fal.ai/static/media/stable-cascade.8406612a.png",  
      "summary": "Stable Cascade: Image generation on a smaller & cheaper latent space.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/stable-cascade"  
    },  
    {  
      "name": "High Quality Stable Video Diffusion",  
      "thumbnail": "https://fal.ai/static/media/stable-video.7039467a.png",  
      "summary": "Generate short video clips from your images using SVD v1.1",  
      "category": "image-to-video",  
      "model_path": "fal-ai/stable-video"  
    },  
    {  
      "name": "Birefnet Background Removal",  
      "thumbnail": "https://fal.ai/static/media/birefnet.7039467a.png",  
      "summary": "bilateral reference framework (BiRefNet) for high-resolution dichotomous image segmentation (DIS)",  
      "category": "image-to-image",  
      "model_path": "fal-ai/birefnet"  
    },  
    {  
      "name": "Creative Upscaler",  
      "thumbnail": "https://fal.ai/static/media/creative-upscaler.7039467a.png",  
      "summary": "Create creative upscaled images.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/creative-upscaler"  
    },  
    {  
      "name": "Clarity Upscaler",  
      "thumbnail": "https://fal.ai/static/media/clarity-upscaler.7039467a.png",  
      "summary": "Clarity upscaler for images with high fidelity.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/clarity-upscaler"  
    },  
    {  
      "name": "CCSR Upscaler",  
      "thumbnail": "https://fal.ai/static/media/ccsr.7039467a.png",  
      "summary": "SOTA Image Upscaler",  
      "category": "image-to-image",  
      "model_path": "fal-ai/ccsr"  
    },  
    {  
      "name": "Stable Diffusion Turbo (v1.5/XL)",  
      "thumbnail": "https://fal.ai/static/media/fast-turbo-diffusion.5313747f.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fast-turbo-diffusion"  
    },  
    {  
      "name": "Latent Consistency Models (v1.5/XL)",  
      "thumbnail": "https://fal.ai/static/media/fast-lcm-diffusion.5313747f.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fast-lcm-diffusion"  
    },  
    {  
      "name": "Whisper",  
      "thumbnail": "https://fal.ai/static/media/whisper.7039467a.png",  
      "summary": "Whisper is a model for speech transcription and translation.",  
      "category": "speech-to-text",  
      "model_path": "fal-ai/whisper"  
    },  
    {  
      "name": "Wizper (Whisper v3 -- fal.ai edition) [Experimental]",  
      "thumbnail": "https://fal.ai/static/media/whisper.7039467a.png",  
      "summary": "Whisper v3 Large -- but optimized by our inference wizards. Same WER, double the performance!",  
      "category": "speech-to-text",  
      "model_path": "fal-ai/wizper"  
    },  
    {  
      "name": "Stable Diffusion XL Lightning",  
      "thumbnail": "https://fal.ai/static/media/fast-lightning-sdxl.5313747f.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fast-lightning-sdxl"  
    },  
    {  
      "name": "Hyper SDXL",  
      "thumbnail": "https://fal.ai/static/media/hyper-sdxl.5313747f.png",  
      "summary": "Hyper-charge SDXL's performance and creativity.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/hyper-sdxl"  
    },  
    {  
      "name": "Playground v2.5",  
      "thumbnail": "https://fal.ai/static/media/playground-v25.d9670780.png",  
      "summary": "State-of-the-art open-source model in aesthetic quality",  
      "category": "text-to-image",  
      "model_path": "fal-ai/playground-v25"  
    },  
    {  
      "name": "AMT Interpolation",  
      "thumbnail": "https://fal.ai/static/media/amt-interpolation.7039467a.png",  
      "summary": "Interpolate between video frames",  
      "category": "video-to-video",  
      "model_path": "fal-ai/amt-interpolation"  
    },  
    {  
      "name": "T2V Turbo - Video Crafter",  
      "thumbnail": "https://fal.ai/static/media/t2v-turbo.24741542.png",  
      "summary": "Generate short video clips from your prompts",  
      "category": "text-to-video",  
      "model_path": "fal-ai/t2v-turbo"  
    },  
    {  
      "name": "SD 1.5 Depth ControlNet",  
      "thumbnail": "https://fal.ai/static/media/sd15-depth-controlnet.7039467a.png",  
      "summary": "SD 1.5 ControlNet",  
      "category": "image-to-image",  
      "model_path": "fal-ai/sd15-depth-controlnet"  
    },  
    {  
      "name": "PhotoMaker",  
      "thumbnail": "https://fal.ai/static/media/photomaker.7039467a.png",  
      "summary": "Customizing Realistic Human Photos via Stacked ID Embedding",  
      "category": "image-to-image",  
      "model_path": "fal-ai/photomaker"  
    },  
    {  
      "name": "Latent Consistency (SDXL & SDv1.5)",  
      "thumbnail": "https://fal.ai/static/media/lcm.7039467a.png",  
      "summary": "Produce high-quality images with minimal inference steps.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/lcm"  
    },  
    {  
      "name": "Optimized Latent Consistency (SDv1.5)",  
      "thumbnail": "https://fal.ai/static/media/lcm.7039467a.png",  
      "summary": "Produce high-quality images with minimal inference steps. Optimized for 512x512 input image size.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/lcm-sd15-i2i"  
    },  
    {  
      "name": "Fooocus",  
      "thumbnail": "https://fal.ai/static/media/fooocus.7039467a.png",  
      "summary": "Default parameters with automated optimizations and quality improvements.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fooocus"  
    },  
    {  
      "name": "AnimateDiff Video-to-Video Evolved",  
      "thumbnail": "https://fal.ai/static/media/animatediff-v2v.7039467a.png",  
      "summary": "Re-animate your videos with evolved consistency!",  
      "category": "video-to-video",  
      "model_path": "fal-ai/animatediff-v2v"  
    },  
    {  
      "name": "AnimateDiff",  
      "thumbnail": "https://fal.ai/static/media/fast-animatediff.7039467a.png",  
      "summary": "Animate your ideas!",  
      "category": "text-to-video",  
      "model_path": "fal-ai/fast-animatediff/text-to-video"  
    },  
    {  
      "name": "AnimateDiff Turbo",  
      "thumbnail": "https://fal.ai/static/media/fast-animatediff.7039467a.png",  
      "summary": "Animate your ideas in lightning speed!",  
      "category": "text-to-video",  
      "model_path": "fal-ai/fast-animatediff/turbo/text-to-video"  
    },  
    {  
      "name": "Illusion Diffusion",  
      "thumbnail": "https://fal.ai/static/media/illusion-diffusion.7039467a.png",  
      "summary": "Create illusions conditioned on image.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/illusion-diffusion"  
    },  
    {  
      "name": "Midas Depth Estimation",  
      "thumbnail": "https://fal.ai/static/media/imageutils.7039467a.png",  
      "summary": "Create depth maps using Midas depth estimation.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/imageutils/depth"  
    },  
    {  
      "name": "Remove Background",  
      "thumbnail": "https://fal.ai/static/media/imageutils.7039467a.png",  
      "summary": "Remove the background from an image.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/imageutils/rembg"  
    },  
    {  
      "name": "Upscale Images",  
      "thumbnail": "https://fal.ai/static/media/esrgan.7039467a.png",  
      "summary": "Upscale images by a given factor.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/esrgan"  
    },  
    {  
      "name": "ControlNet SDXL",  
      "thumbnail": "https://fal.ai/static/media/fast-sdxl-controlnet-canny.5313747f.png",  
      "summary": "Generate Images with ControlNet.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/fast-sdxl-controlnet-canny"  
    },  
    {  
      "name": "Inpainting sdxl and sd",  
      "thumbnail": "https://fal.ai/static/media/inpaint.7039467a.png",  
      "summary": "Inpaint images with SD and SDXL",  
      "category": "image-to-image",  
      "model_path": "fal-ai/inpaint"  
    },  
    {  
      "name": "Animatediff SparseCtrl LCM",  
      "thumbnail": "https://fal.ai/static/media/animatediff-sparsectrl-lcm.7039467a.png",  
      "summary": "Animate Your Drawings with Latent Consistency Models!",  
      "category": "text-to-video",  
      "model_path": "fal-ai/animatediff-sparsectrl-lcm"  
    },  
    {  
      "name": "Swap Face",  
      "thumbnail": "https://fal.ai/static/media/face-swap.7039467a.png",  
      "summary": "Swap a face between two images.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/face-swap"  
    },  
    {  
      "name": "PuLID",  
      "thumbnail": "https://fal.ai/static/media/pulid.7039467a.png",  
      "summary": "Tuning-free ID customization.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/pulid"  
    },  
    {  
      "name": "IP Adapter Face ID",  
      "thumbnail": "https://fal.ai/static/media/ip-adapter-face-id.7039467a.png",  
      "summary": "High quality zero-shot personalization",  
      "category": "image-to-image",  
      "model_path": "fal-ai/ip-adapter-face-id"  
    },  
    {  
      "name": "Marigold Depth Estimation",  
      "thumbnail": "https://fal.ai/static/media/imageutils.7039467a.png",  
      "summary": "Create depth maps using Marigold depth estimation.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/imageutils/marigold-depth"  
    },  
    {  
      "name": "Stable Audio Open",  
      "thumbnail": "https://fal.ai/static/media/stable-audio.7039467a.png",  
      "summary": "Open source text-to-audio model.",  
      "category": "text-to-audio",  
      "model_path": "fal-ai/stable-audio"  
    },  
    {  
      "name": "DiffusionEdge",  
      "thumbnail": "https://fal.ai/static/media/diffusion-edge.7039467a.png",  
      "summary": "Diffusion based high quality edge detection",  
      "category": "text-to-image",  
      "model_path": "fal-ai/diffusion-edge"  
    },  
    {  
      "name": "TripoSR",  
      "thumbnail": "https://fal.ai/static/media/triposr.7039467a.png",  
      "summary": "State of the art Image to 3D Object generation",  
      "category": "image-to-3d",  
      "model_path": "fal-ai/triposr"  
    },  
    {  
      "name": "Face Retoucher",  
      "thumbnail": "https://fal.ai/static/media/retoucher.7039467a.png",  
      "summary": "Automatically retouches faces to smooth skin and remove blemishes.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/retoucher"  
    },  
    {  
      "name": "LLaVA v1.5 13B",  
      "thumbnail": "https://fal.ai/static/media/llavav15-13b.7039467a.png",  
      "summary": "Vision",  
      "category": "vision",  
      "model_path": "fal-ai/llavav15-13b"  
    },  
    {  
      "name": "LLaVA v1.6 34B",  
      "thumbnail": "https://fal.ai/static/media/llava-next.7039467a.png",  
      "summary": "Vision",  
      "category": "vision",  
      "model_path": "fal-ai/llava-next"  
    },  
    {  
      "name": "NSFW Filter",  
      "thumbnail": "https://fal.ai/static/media/imageutils.7039467a.png",  
      "summary": "Predict the probability of an image being NSFW.",  
      "category": "vision",  
      "model_path": "fal-ai/imageutils/nsfw"  
    },  
    {  
      "name": "SUPIR Upscaler",  
      "thumbnail": "https://fal.ai/static/media/supir.7039467a.png",  
      "summary": "A Powerful Image Upscaler",  
      "category": "image-to-image",  
      "model_path": "fal-ai/supir"  
    },  
    {  
      "name": "Face to Sticker",  
      "thumbnail": "https://fal.ai/static/media/face-to-sticker.7039467a.png",  
      "summary": "Create stickers from faces.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/face-to-sticker"  
    },  
    {  
      "name": "Moondream",  
      "thumbnail": "https://fal.ai/static/media/moondream.7039467a.png",  
      "summary": "Answer questions from the images.",  
      "category": "vision",  
      "model_path": "fal-ai/moondream/batched"  
    },  
    {  
      "name": "Sad Talker",  
      "thumbnail": "https://fal.ai/static/media/sadtalker.7039467a.png",  
      "summary": "Learning Realistic 3D Motion Coefficients for Stylized Audio-Driven Single Image Talking Face Animation",  
      "category": "image-to-video",  
      "model_path": "fal-ai/sadtalker"  
    },  
    {  
      "name": "Stable Diffusion with LoRAs",  
      "thumbnail": "https://fal.ai/static/media/lora.7039467a.png",  
      "summary": "Run Any Stable Diffusion model with customizable LoRA weights.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/lora/image-to-image"  
    },  
    {  
      "name": "Stable Diffusion XL",  
      "thumbnail": "https://fal.ai/static/media/fast-sdxl.5313747f.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "image-to-image",  
      "model_path": "fal-ai/fast-sdxl/image-to-image"  
    },  
    {  
      "name": "Stable Diffusion XL",  
      "thumbnail": "https://fal.ai/static/media/fast-sdxl.5313747f.png",  
      "summary": "Run SDXL at the speed of light",  
      "category": "image-to-image",  
      "model_path": "fal-ai/fast-sdxl/inpainting"  
    },  
    {  
      "name": "Stable Diffusion with LoRAs",  
      "thumbnail": "https://fal.ai/static/media/lora.7039467a.png",  
      "summary": "Run Any Stable Diffusion model with customizable LoRA weights.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/lora/inpaint"  
    },  
    {  
      "name": "PixArt-Σ",  
      "thumbnail": "https://fal.ai/static/media/pixart-sigma.7039467a.png",  
      "summary": "Weak-to-Strong Training of Diffusion Transformer for 4K Text-to-Image Generation",  
      "category": "text-to-image",  
      "model_path": "fal-ai/pixart-sigma"  
    },  
    {  
      "name": "Dreamshaper",  
      "thumbnail": "https://fal.ai/static/media/dreamshaper.7039467a.png",  
      "summary": "Dreamshaper model.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/dreamshaper"  
    },  
    {  
      "name": "Realistic Vision",  
      "thumbnail": "https://fal.ai/static/media/realistic-vision.7039467a.png",  
      "summary": "Generate realistic images.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/realistic-vision"  
    },  
    {  
      "name": "Lightning Models",  
      "thumbnail": "https://fal.ai/static/media/lightning-models.7039467a.png",  
      "summary": "Collection of SDXL Lightning models.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/lightning-models"  
    },  
    {  
      "name": "Omni Zero",  
      "thumbnail": "https://fal.ai/static/media/omni-zero.7039467a.png",  
      "summary": "Any pose, any style, any identity",  
      "category": "image-to-image",  
      "model_path": "fal-ai/omni-zero"  
    },  
    {  
      "name": "Virtual Try-On",  
      "thumbnail": "https://fal.ai/static/media/cat-vton.7039467a.png",  
      "summary": "Image based Virtual Try-On",  
      "category": "image-to-image",  
      "model_path": "fal-ai/cat-vton"  
    },  
    {  
      "name": "DWPose Pose Prediction",  
      "thumbnail": "https://fal.ai/static/media/dwpose.7039467a.png",  
      "summary": "Predict poses.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/dwpose"  
    },  
    {  
      "name": "SoteDiffusion",  
      "thumbnail": "https://fal.ai/static/media/stable-cascade.7039467a.png",  
      "summary": "Anime finetune of Würstchen V3.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/stable-cascade/sote-diffusion"  
    },  
    {  
      "name": "Florence-2 Large",  
      "thumbnail": "https://fal.ai/static/media/florence-2-large.7039467a.png",  
      "summary": "Florence-2 is an advanced vision foundation model that uses a prompt-based approach to handle a wide range of vision and vision-language tasks",  
      "category": "image-to-text",  
      "model_path": "fal-ai/florence-2-large/caption"  
    },  
    {  
      "name": "Live Portrait",  
      "thumbnail": "https://fal.ai/static/media/live-portrait.7039467a.png",  
      "summary": "Transfer expression from a video to a portrait.",  
      "category": "image-to-video",  
      "model_path": "fal-ai/live-portrait"  
    },  
    {  
      "name": "MusePose",  
      "thumbnail": "https://fal.ai/static/media/muse-pose.7039467a.png",  
      "summary": "Animate a reference image with a driving video using MusePose.",  
      "category": "video-to-video",  
      "model_path": "fal-ai/muse-pose"  
    },  
    {  
      "name": "Kolors",  
      "thumbnail": "https://fal.ai/static/media/kolors.7039467a.png",  
      "summary": "Photorealistic Text-to-Image",  
      "category": "text-to-image",  
      "model_path": "fal-ai/kolors"  
    },  
    {  
      "name": "SDXL ControlNet Union",  
      "thumbnail": "https://fal.ai/static/media/sdxl-controlnet-union.7039467a.png",  
      "summary": "An efficent SDXL multi-controlnet text-to-image model.",  
      "category": "text-to-image",  
      "model_path": "fal-ai/sdxl-controlnet-union"  
    },  
    {  
      "name": "SDXL ControlNet Union",  
      "thumbnail": "https://fal.ai/static/media/sdxl-controlnet-union.7039467a.png",  
      "summary": "An efficent SDXL multi-controlnet image-to-image model.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/sdxl-controlnet-union/image-to-image"  
    },  
    {  
      "name": "SDXL ControlNet Union",  
      "thumbnail": "https://fal.ai/static/media/sdxl-controlnet-union.7039467a.png",  
      "summary": "An efficent SDXL multi-controlnet inpainting model.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/sdxl-controlnet-union/inpainting"  
    },  
    {  
      "name": "Segment Anything Model 2",  
      "thumbnail": "https://fal.ai/static/media/sam2.7039467a.png",  
      "summary": "SAM 2 is a model for segmenting images and videos in real-time.",  
      "category": "image-to-image",  
      "model_path": "fal-ai/sam2/image"  
    }  
  ]  

let msnry = null; // Masonry instance variable - MOVED TO GLOBAL SCOPE
let uploadthingClient = null; // Will hold the UploadThing client instance

// Initialize UploadThing Client
function initUploadThing() {
    try {
        console.log("Attempting to initialize UploadThing client");
        
        // Debug what's available
        if (window.uploadthing) {
            console.log("Found window.uploadthing, methods:", Object.keys(window.uploadthing));
        } else if (window.UploadThing) {
            console.log("Found window.UploadThing, methods:", Object.keys(window.UploadThing));
        } else if (window.UT) {
            console.log("Found window.UT, methods:", Object.keys(window.UT));
        } else {
            console.log("No UploadThing global object found. Available globals:", 
                Object.keys(window).filter(k => k.toLowerCase().includes('upload')));
        }
        
        // Try different possible API variations
        if (window.uploadthing && typeof window.uploadthing.createUploadthing === 'function') {
            uploadthingClient = window.uploadthing.createUploadthing({
                url: '/api/uploadthing',
            });
        } else if (window.uploadthing && typeof window.uploadthing.createFileUploader === 'function') {
            // Some versions directly expose createFileUploader
            uploadthingClient = {
                createFileUploader: (options) => window.uploadthing.createFileUploader({
                    ...options,
                    url: '/api/uploadthing'
                })
            };
        } else if (window.UploadThing && typeof window.UploadThing.createUploader === 'function') {
            // Try alternative global name
            uploadthingClient = window.UploadThing.createUploader({
                url: '/api/uploadthing'
            });
        } else {
            // Fallback to direct fetch implementation if no library methods work
            console.warn("Could not find UploadThing client methods. Using fallback implementation.");
            uploadthingClient = createFallbackUploader();
        }
        
        console.log("UploadThing client initialized successfully");
        return true;
    } catch (error) {
        console.error("Error initializing UploadThing client:", error);
        // Fall back to direct upload implementation
        uploadthingClient = createFallbackUploader();
        return uploadthingClient !== null;
    }
}

// Create a fallback uploader that uses direct API endpoint
function createFallbackUploader() {
    console.warn("Using fallback direct upload implementation instead of UploadThing");
    
    return {
        createFileUploader: ({ endpoint, onUploadProgress }) => {
            return async (files) => {
                // Only upload the first file for simplicity
                const file = files[0];
                if (!file) {
                    throw new Error("No file provided");
                }
                
                const formData = new FormData();
                formData.append('file', file);
                
                const xhr = new XMLHttpRequest();
                xhr.open('POST', '/api/direct-upload'); // Use our fallback endpoint
                
                // Set up progress handling
                xhr.upload.onprogress = (event) => {
                    if (event.lengthComputable && onUploadProgress) {
                        const progress = Math.round((event.loaded / event.total) * 100);
                        onUploadProgress({ progress });
                    }
                };
                
                // Return a promise that resolves with the response
                return new Promise((resolve, reject) => {
                    xhr.onload = () => {
                        if (xhr.status >= 200 && xhr.status < 300) {
                            try {
                                const response = JSON.parse(xhr.responseText);
                                console.log("Fallback upload successful:", response);
                                resolve(response); // Should be in format [{ url: "..." }]
                            } catch (e) {
                                reject(new Error("Invalid JSON response from fallback upload server"));
                            }
                        } else {
                            reject(new Error(`Fallback upload failed with status ${xhr.status}`));
                        }
                    };
                    xhr.onerror = () => reject(new Error("Network error during fallback upload"));
                    xhr.send(formData);
                });
            };
        }
    };
}

// Function to upload a file using UploadThing
async function uploadFileToUploadThing(file) {
    // Validate input
    if (!file || !(file instanceof File)) {
        throw new Error("Invalid file provided for upload");
    }
    
    // Show upload progress UI
    const uploadProgress = document.getElementById('uploadProgress');
    const uploadStatus = document.getElementById('uploadStatus');
    const uploadProgressBar = document.getElementById('uploadProgressBar');
    
    if (uploadProgress) uploadProgress.classList.remove('hidden');
    if (uploadStatus) uploadStatus.textContent = 'Preparing upload...';
    if (uploadProgressBar) uploadProgressBar.value = 0;
    
    try {
        // Check if client is initialized and initialize if needed
        if (!uploadthingClient) {
            console.warn("UploadThing client not initialized, trying to initialize...");
            const initSuccess = initUploadThing();
            if (!initSuccess || !uploadthingClient) {
                throw new Error("Failed to initialize UploadThing client");
            }
        }
        
        console.log("Creating file uploader with endpoint 'imageUploader'");
        // Try/catch specifically around the createFileUploader step for better error diagnosis
        let endpoint;
        try {
            // Create the endpoint uploader 
            endpoint = uploadthingClient.createFileUploader({
                endpoint: "imageUploader", // Must match the server-side fileRouter endpoint
                onUploadProgress: ({ progress }) => {
                    console.log(`Upload progress: ${progress}%`);
                    if (uploadProgressBar) uploadProgressBar.value = progress;
                    if (uploadStatus) uploadStatus.textContent = `Uploading... ${progress}%`;
                }
            });
            console.log("File uploader created successfully");
        } catch (endpointError) {
            console.error("Failed to create file uploader:", endpointError);
            throw new Error(`Could not create uploader: ${endpointError.message}`);
        }
        
        // Start the upload with the file
        console.log("Starting file upload...");
        const response = await endpoint([file]);
        
        console.log("Upload successful:", response);
        
        // Update UI to indicate success
        if (uploadStatus) uploadStatus.textContent = 'Upload complete!';
        if (uploadProgressBar) uploadProgressBar.value = 100;
        
        // Hide progress after a short delay
        setTimeout(() => {
            if (uploadProgress) uploadProgress.classList.add('hidden');
        }, 1500);
        
        // Get the URL from the response
        let fileUrl = null;
        
        if (response && Array.isArray(response) && response.length > 0 && response[0].url) {
            fileUrl = response[0].url;
        } else if (response && response.url) {
            fileUrl = response.url;
        } else if (response && typeof response === 'string' && response.startsWith('http')) {
            fileUrl = response;
        } else {
            console.warn("Unexpected response format from uploadthing:", response);
            throw new Error("Upload succeeded but response format was unexpected");
        }
        
        console.log("Final uploaded file URL:", fileUrl);
        
        // Store the URL in our hidden input
        const uploadedImageUrl = document.getElementById('uploadedImageUrl');
        if (uploadedImageUrl && fileUrl) {
            uploadedImageUrl.value = fileUrl;
        }
        
        return fileUrl;
    } catch (error) {
        console.error("Error uploading file:", error);
        
        // Update UI to indicate error
        if (uploadStatus) uploadStatus.textContent = `Upload failed: ${error.message || 'Unknown error'}`;
        if (uploadProgress) {
            // Add error styling
            uploadProgress.classList.remove('hidden');
            uploadProgressBar.classList.remove('progress-primary');
            uploadProgressBar.classList.add('progress-error');
        }
        
        // Hide progress after a longer delay
        setTimeout(() => {
            if (uploadProgress) uploadProgress.classList.add('hidden');
        }, 3000);
        
        throw error;
    }
}

// Function to display ALL results in the main preview pane
function displayResultImages(imageArray) { // Renamed parameter
    const imagePlaceholder = document.getElementById('imagePlaceholder');
    imagePlaceholder.innerHTML = ''; // Clear previous results

    if (!imageArray || !Array.isArray(imageArray) || imageArray.length === 0) {
        console.warn("displayResultImages: Invalid or empty image array received.", imageArray);
        imagePlaceholder.innerHTML = '<p class="text-center text-error">No result images to display.</p>';
        return;
    }

    console.log(`Displaying ${imageArray.length} result images.`);

    // Loop through each image object in the array
    imageArray.forEach((imageObj, index) => {
        if (!imageObj || !imageObj.url) {
            console.warn("Skipping invalid image object in array:", imageObj);
            return; // Skip this one
        }

        const imgContainer = document.createElement('div');
        imgContainer.className = 'image-container mb-4'; // Add margin between multiple images

        const img = document.createElement('img');
        img.src = imageObj.url;
        img.alt = `Generated Result ${index + 1}`;

        const downloadBtn = document.createElement('button');
        downloadBtn.textContent = 'Download'; // Shorter text
        downloadBtn.className = 'btn btn-sm btn-accent download-btn'; 
        downloadBtn.addEventListener('click', () => {
            // Use index for unique filename
            const filename = `result-${index + 1}-${Date.now()}.png`; 
            downloadImage(imageObj.url, filename)
        });

        imgContainer.appendChild(img);
        imgContainer.appendChild(downloadBtn);
        imagePlaceholder.appendChild(imgContainer); // Append each container
    });
}

// Make sure downloadImage function exists (it might have been removed earlier)
function downloadImage(url, filename) {
    fetch(url)
        .then(response => {
            if (!response.ok) throw new Error(`Network response was not ok: ${response.statusText}`);
            return response.blob();
        })
        .then(blob => {
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = filename;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            URL.revokeObjectURL(link.href); // Clean up blob URL
        })
        .catch(error => console.error('Error downloading image:', error));
}

document.addEventListener('DOMContentLoaded', async function() {
    // ===== Ensure UploadThing is loaded =====
    if (!window.uploadthing && !window.UploadThing) {
        console.log("UploadThing not detected, attempting to load it dynamically");
        try {
            // Try to load UploadThing dynamically if not already available
            await new Promise((resolve, reject) => {
                const script = document.createElement('script');
                script.src = 'https://unpkg.com/uploadthing@5.7.4/dist/uploadthing.js';
                script.onload = () => {
                    console.log("UploadThing script dynamically loaded");
                    resolve();
                };
                script.onerror = () => {
                    console.error("Failed to dynamically load UploadThing");
                    reject(new Error("Failed to load UploadThing script"));
                };
                document.head.appendChild(script);
            });
            
            // Initialize after loading
            initUploadThing();
        } catch (error) {
            console.error("Error loading UploadThing:", error);
        }
    } else {
        console.log("UploadThing already available, initializing...");
        initUploadThing();
    }
    // ===== End UploadThing loading =====

    // Initialize UploadThing client
    initUploadThing();
    
    // Add clipboard paste functionality
    setupClipboardPaste();
    
    // Add near top with other state variables
    // let msnry = null; // Masonry instance variable - REMOVED FROM HERE
    const historyItemSelector = '.history-entry'; // Selector for grid items

    const numStepsSlider = document.getElementById('numStepsSlider');
    const numStepsInput = document.getElementById('numStepsInput');
  
    // Update number input when slider changes
    numStepsSlider.addEventListener('input', function() {
      numStepsInput.value = this.value;
    });
  
    // Update slider when number input changes
    numStepsInput.addEventListener('change', function() {
      numStepsSlider.value = this.value;
    });
  
    // Ensure the number input stays within the slider's range
    numStepsInput.addEventListener('input', function() {
      let value = parseInt(this.value);
      if (value < 1) this.value = 1;
      if (value > 50) this.value = 50;
    });

    const guidanceScaleSlider = document.getElementById('guidanceScaleSlider');
    const guidanceScaleInput = document.getElementById('guidanceScaleInput');

    // Update number input when slider changes
    guidanceScaleSlider.addEventListener('input', function() {
        guidanceScaleInput.value = this.value;
    });

    // Update slider when number input changes
    guidanceScaleInput.addEventListener('change', function() {
        guidanceScaleSlider.value = this.value;
    });

    // Ensure the number input stays within the slider's range
    guidanceScaleInput.addEventListener('input', function() {
        let value = parseFloat(this.value);
        if (value < 1) this.value = 1;
        if (value > 10) this.value = 10;
    });

    const form = document.getElementById('generationForm');
    const generateBtn = document.getElementById('generateBtn');
    const loadingSpinner = document.getElementById('loadingSpinner');
    const logsContainer = document.getElementById('logs');
    const imagePlaceholder = document.getElementById('imagePlaceholder');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    let totalImages = 0;
    let imagesGenerated = 0;

    // --- Add Selectors for new UI elements ---
    const imageUploadContainer = document.getElementById('imageUploadContainer');
    const imageUploadInput = document.getElementById('imageUploadInput');
    const imagePreview = document.getElementById('imagePreview');
    const strengthContainer = document.getElementById('strengthContainer');
    const strengthSlider = document.getElementById('strengthSlider');
    const strengthInput = document.getElementById('strengthInput');
    const strengthValue = document.getElementById('strengthValue'); // Span to display value
    // --- End Selectors ---

    const imageSizeContainer = document.getElementById('imageSizeContainer');
    const aspectRatioContainer = document.getElementById('aspectRatioContainer');
    const modelSelect = document.getElementById('modelSelect');
    const baseModelContainer = document.getElementById('baseModelContainer');
    const historyContainer = document.getElementById('history');
    const thumbnailSizeSlider = document.getElementById('thumbnailSizeSlider');
    const thumbnailSizeLabel = document.getElementById('thumbnailSizeLabel');

    // --- Thumbnail Size Slider Logic ---
    const sizeMap = { // Map slider values to labels and CLASS NAMES
        1: { label: "Small", className: 'w-1/6' },
        2: { label: "Medium", className: 'w-1/4' },
        3: { label: "Large", className: 'w-1/3' },
        4: { label: "X-Large", className: 'w-1/2' }
    };
    let currentSizeClass = sizeMap[2].className; // Default size class

    function setHistoryItemSize(sliderValue) {
        const setting = sizeMap[sliderValue];
        if (historyContainer && setting) {
            const newSizeClass = setting.className;
            
            // Update label
            if (thumbnailSizeLabel) {
                thumbnailSizeLabel.textContent = setting.label;
            }

            // If class hasn't changed, do nothing
            if (newSizeClass === currentSizeClass) return; 

            console.log(`Changing history item size class from ${currentSizeClass} to ${newSizeClass}`);

            // Update all existing items
            const items = historyContainer.querySelectorAll('.history-entry');
            items.forEach(item => {
                item.classList.remove(currentSizeClass); // Remove old class
                item.classList.add(newSizeClass);      // Add new class
            });

            currentSizeClass = newSizeClass; // Update current class tracking

            // Tell Masonry to re-layout
            if (msnry) {
                console.log("Triggering Masonry layout due to size change.");
                msnry.layout();
            }

        } else {
            console.warn("Could not set history item size. Container or setting not found for value:", sliderValue);
        }
    }

    // Set initial size class (but don't apply to items yet)
    if (thumbnailSizeSlider) {
        const initialSetting = sizeMap[thumbnailSizeSlider.value];
        if (initialSetting) {
            currentSizeClass = initialSetting.className;
            if (thumbnailSizeLabel) thumbnailSizeLabel.textContent = initialSetting.label;
        }
    }

    // Add event listener for real-time updates
    if (thumbnailSizeSlider) {
        thumbnailSizeSlider.addEventListener('input', (event) => {
            setHistoryItemSize(event.target.value);
        });
    }
    // --- End Thumbnail Size Slider Logic ---

    // Function to initialize Masonry
    function initMasonry() {
        if (msnry) {
            console.log("Destroying existing Masonry instance.");
            msnry.destroy(); // Destroy previous instance if exists
        }
        imagesLoaded(historyContainer, function() {
            console.log("Images loaded, initializing Masonry...");
            msnry = new Masonry(historyContainer, {
                itemSelector: '.history-entry',
                percentPosition: true, // Good for percentage widths
                gutter: 0 // Use padding on items for gutter
            });
            console.log("Masonry initialized.");
        });
    }

    // Function to update dropdown visibility
    function updateDropdownVisibility() {
        const selectedModel = modelSelect.value;
        console.log(`updateDropdownVisibility called. Selected Model: "${selectedModel}"`); // Log function call and value

        // Helper function to safely set display style
        const setDisplay = (element, displayValue) => {
            if (element) {
                element.style.display = displayValue;
            } else {
                // Optionally log a warning if an element is missing, but don't crash
                // console.warn(`Attempted to set display for a non-existent element.`);
            }
        };

        // Hide all conditional containers initially using the safe helper
        setDisplay(imageSizeContainer, 'none');
        setDisplay(aspectRatioContainer, 'none');
        setDisplay(baseModelContainer, 'none');
        setDisplay(imageUploadContainer, 'none');
        setDisplay(strengthContainer, 'none');

        // Show containers based on model, using the safe helper
        if (selectedModel === 'fal-ai/flux-pro/v1.1-ultra') {
            console.log('Showing aspectRatioContainer');
            setDisplay(aspectRatioContainer, 'block');
        } else if (selectedModel === 'fal-ai/lora') {
            console.log('Showing imageSizeContainer AND baseModelContainer for fal-ai/lora'); // Log specific path
            setDisplay(imageSizeContainer, 'block');
            setDisplay(baseModelContainer, 'block');
        } else if (selectedModel === 'fal-ai/flux/dev/image-to-image') { // *** NEW MODEL CASE ***
            console.log('Showing imageUploadContainer and strengthContainer for fal-ai/flux/dev/image-to-image');
            setDisplay(imageUploadContainer, 'block');
            setDisplay(strengthContainer, 'block');
            // Optionally hide others if needed
            // setDisplay(imageSizeContainer, 'none');
        } else if (selectedModel) { // For any other selected model (that's not empty)
            console.log('Showing imageSizeContainer for other model:', selectedModel);
            setDisplay(imageSizeContainer, 'block');
        } else {
             console.log('No model selected, hiding all conditional containers.');
        }
        // If no model is selected (initial state), all remain hidden
    }

    // Add event listener to model select
    if (modelSelect) {
        modelSelect.addEventListener('change', () => { // Wrap listener to add log
            console.log('#modelSelect change event fired.');
            updateDropdownVisibility();
        });
    }

    // Initial call to set correct visibility on page load (after models are loaded)
    // We need to ensure this runs *after* loadModels populates the select
    // Let's modify loadModels to call this function at the end

    form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const formData = new FormData(e.target);

        // --- Build queryParams selectively ---
        let queryParams = new URLSearchParams();
        const selectedModelEndpoint = formData.get('model');
        const promptValue = formData.get('prompt'); // Get prompt early for checks

        // --- Image Upload Handling (Placeholder) ---
        let imageUrlForApi = null; // Will hold the URL after upload
        if (selectedModelEndpoint === 'fal-ai/flux/dev/image-to-image') {
            const imageFile = imageUploadInput.files[0];
            if (!imageFile) {
                alert('Please select an input image for the Image-to-Image model.');
                return; // Stop submission if image is required but missing
            }
            if (!promptValue) {
                 alert('Please enter a prompt.');
                 return; // Stop submission if prompt is missing
            }

            // --- UploadThing Integration --- 
            // Now, get the URL from our hidden input field which has been populated
            // when the file was uploaded via the input's change event
            try {
                const uploadedImageUrl = document.getElementById('uploadedImageUrl');
                if (!uploadedImageUrl || !uploadedImageUrl.value) {
                    throw new Error("No uploaded image URL found. Please upload an image first.");
                }
                
                imageUrlForApi = uploadedImageUrl.value;
                
                console.log("Using previously uploaded image URL:", imageUrlForApi);
                
                if (!imageUrlForApi) {
                    throw new Error("Could not retrieve the uploaded image URL.");
                }
                
            } catch (uploadError) {
                 console.error("Error retrieving uploaded image URL:", uploadError);
                 alert(`Image URL retrieval failed: ${uploadError.message}`);
                 return; // Stop if obtaining the URL fails
            }
            // --- End UploadThing Integration ---
        }
        // --- End Image Upload Handling ---


        // Set default/form values initially
        let numSteps = formData.get('numStepsInput') || formData.get('numStepsSlider');
        let guidanceScale = formData.get('guidanceScaleInput') || formData.get('guidanceScaleSlider');
        let imageSize = formData.get('imageSize');
        let aspectRatio = formData.get('aspectRatio');
        const loraUrl = formData.get('loraUrl');
        const loraScale = formData.get('loraScale') || '1';
        let includeLoras = !!loraUrl; // Flag to check if LoRAs should be included
        const baseModelName = formData.get('baseModelName'); // Get selected base model name
        const strength = formData.get('strengthInput') || formData.get('strengthSlider'); // Get strength value

        // Append common parameters first
        queryParams.append('model', selectedModelEndpoint);
        queryParams.append('prompt', promptValue);
        queryParams.append('seed', formData.get('seed') || 'random');
        queryParams.append('numImages', formData.get('numImages') || '1');
        queryParams.append('enableSafetyChecker', formData.get('enableSafetyChecker') ? 'true' : 'false');


        // --- Model-Specific Parameter Adjustments ---
        if (selectedModelEndpoint === 'fal-ai/flux/dev/image-to-image') {
             if (!imageUrlForApi) { // Double check URL exists before appending
                 alert("Internal error: Image URL not available after upload attempt.");
                 return;
             }
             queryParams.append('image_url', imageUrlForApi);
             queryParams.append('strength', strength);
             // These might not be applicable or have different defaults for Img2Img
             // queryParams.append('numSteps', numSteps);
             // queryParams.append('guidanceScale', guidanceScale);
             // Check Fal AI docs for specific parameters for this endpoint
             // From docs: num_inference_steps (default 40), guidance_scale (default 3.5) are available
             queryParams.append('num_inference_steps', numSteps); // Map our UI numSteps to this
             queryParams.append('guidance_scale', guidanceScale); // Map our UI guidanceScale to this

             includeLoras = false; // LoRAs might not be directly supported on this specific endpoint
        } else if (selectedModelEndpoint === 'fal-ai/flux-pro/v1.1-ultra') {
            queryParams.append('aspectRatio', aspectRatio);
            includeLoras = false; 
        } else if (selectedModelEndpoint === 'fal-ai/fast-turbo-diffusion') {
            numSteps = '4'; 
            guidanceScale = '1.0'; 
            queryParams.append('imageSize', imageSize);
            includeLoras = false; 
        } else if (selectedModelEndpoint === 'fal-ai/lora') {
             queryParams.append('imageSize', imageSize); // Use the string enum like 'square_hd'
             if (baseModelName) { // Send the selected base model name to backend
                queryParams.append('baseModelName', baseModelName);
             }
             // includeLoras remains true if loraUrl was provided
        } else { // Default for other models 
            queryParams.append('imageSize', imageSize); // Assuming others use string enums too, adjust if needed
            includeLoras = false; 
        }

        // Append adjusted/common numerical parameters
        queryParams.append('numSteps', numSteps);
        queryParams.append('guidanceScale', guidanceScale);

        // Append LoRA parameters only if applicable for the selected model and provided
        if (includeLoras && loraUrl) {
            queryParams.append('loraUrl', loraUrl);
            queryParams.append('loraScale', loraScale);
        }
        // --- End Model-Specific Adjustments ---


        const queryString = queryParams.toString();
        console.log("QueryString:", queryString); // Log the query string for debugging
        // --- End selective build ---

        totalImages = parseInt(formData.get('numImages'), 10) || 1;
        imagesGenerated = 0;

        // Show loading spinner and disable generate button
        loadingSpinner.style.display = 'inline-block';
        generateBtn.disabled = true;

        // Clear previous logs and images
        logsContainer.innerHTML = '';
        imagePlaceholder.innerHTML = '';

        const eventSource = new EventSource(`/generate-stream?${queryString}`); // Use the selectively built queryString

        eventSource.onmessage = function(event) {
            try { // Added try...catch block for robust parsing
                const data = JSON.parse(event.data);
                console.log('Received data:', data);

                if (data.type === 'log') {
                    appendLog(data.message);
                } else if (data.type === 'result') {
                    imagesGenerated++;
                    
                    let images = null;
                    
                    if (data.data && Object.keys(data.data).length > 0) {
                        if (Array.isArray(data.data.images) && data.data.images.length > 0) {
                            images = data.data.images;
                        } else if (data.data.image) { 
                            images = [data.data.image];
                        } else if (data.data.url) { 
                             images = [{ url: data.data.url }];
                        }
                         else if (!images) {
                           console.error('Result received, but data.data is missing image information:', data.data);
                           appendLog('Error: Received result from server, but it did not contain image data. Check server logs.');
                        }
                    }
                    else if (Array.isArray(data.images) && data.images.length > 0) { images = data.images; }
                    else if (data.image) { images = [data.image]; }
                    else if (data.url) { images = [{ url: data.url }]; }

                    // --- Check images array and call displayResultImages --- 
                    if (images && images.length > 0) { 
                        
                        // --- Display ALL images in the result pane --- 
                        displayResultImages(images); // Pass the whole array
                        // --- End display ---

                        // --- Handle history (unchanged) ---
                        console.log("Attempting to handle final stream data:", data.data && Object.keys(data.data).length > 0 ? data.data : data); 
                        handleFinalStream(data.data && Object.keys(data.data).length > 0 ? data.data : data); 
                        // --- End history handling ---

                    } else {
                        // Error logged above if data.data was present but empty
                        if (!(data.data && Object.keys(data.data).length > 0)) {
                           console.error('Could not extract valid image data from received result (data.data missing or empty):', data);
                           appendLog('Error: Could not find valid image data in response structure. Check server logs.');
                           // Maybe display an error image in the result pane?
                           displayResultImages(null); // Pass null to clear/show error
                        }
                    }
                    // --- End Check --- 
                    
                    if (imagesGenerated === totalImages) {
                        eventSource.close();
                        loadingSpinner.style.display = 'none';
                        generateBtn.disabled = false;
                    }
                } else if (data.type === 'error') {
                    // Handle server-sent errors (improved)
                    let errorMessage = data.message || 'Unknown error occurred on server';
                    console.error('API Error Reported:', data);
                    if (data.details && data.status === 422) {
                         if (Array.isArray(data.details.detail)) {
                            const validationErrors = data.details.detail.map(err => {
                                return `Field '${err.loc.join('.')}': ${err.msg}`;
                            }).join('\n');
                            errorMessage = `Validation error: ${validationErrors}`;
                         }
                    }
                    appendLog(`Error: ${errorMessage.replace(/\n/g, '<br>')}`); // Display multiline errors
                    eventSource.close();
                    loadingSpinner.style.display = 'none';
                    generateBtn.disabled = false;
                } else {
                    console.warn('Unknown data type received:', data);
                }
            } catch (error) {
                console.error('Error processing message:', error, event.data);
                appendLog('Error processing server response. Invalid JSON? See console.');
                eventSource.close();
                loadingSpinner.style.display = 'none';
                generateBtn.disabled = false;
            }
        };

        eventSource.onerror = function(error) {
            console.error('EventSource failed:', error);
            appendLog('Connection error. Please try again.');
            eventSource.close();
            // Hide loading spinner and enable generate button
            loadingSpinner.style.display = 'none';
            generateBtn.disabled = false;
        };
    });

    function appendLog(message) {
        const progressMatch = message.match(/(\d+)%/);
        if (progressMatch) {
            const progress = parseInt(progressMatch[1], 10);
            const overallProgress = Math.min((imagesGenerated - 1) / totalImages * 100 + progress / totalImages, 100);
        }
    }

    let allHistoryImages = []; // Keep track of images displayed in the modal
    let currentHistoryImageIndex = 0;
    let currentHistoryPage = 1; // Track current page for infinite scroll
    let isLoadingHistory = false; // Prevent multiple loads
    let hasMoreHistory = true; // Assume more pages initially
    let currentHistoryEndpoint = null; // Track the endpoint being scrolled

    function handleFinalStream(data) {
        console.log("Inside handleFinalStream with data:", data); // Log entry
        const logsContainer = document.getElementById('logs');
        const logEntry = document.createElement('div');

        try { 
            const inferenceTime = data.timings && data.timings.inference ? data.timings.inference.toFixed(2) + ' seconds' : 'N/A';
            const prompt = data.prompt || '[Prompt missing]';
            const seed = data.seed || '[Seed missing]';
            const has_nsfw_concepts = data.has_nsfw_concepts !== undefined ? data.has_nsfw_concepts : '[NSFW status missing]';
            const images = (data.images && Array.isArray(data.images)) ? data.images : []; // Keep full image objects

            if (images.length === 0) {
                 console.warn("handleFinalStream: No image objects found in the data.", data);
                 return; 
            }

            logEntry.textContent = `Prompt: ${prompt}, Seed: ${seed}, NSFW Concepts: ${has_nsfw_concepts}, Timings: Inference - ${inferenceTime}`;
            logsContainer.appendChild(logEntry);
            logsContainer.scrollTop = logsContainer.scrollHeight;

            // Instead of saving to DB, directly add to DOM
            images.forEach(image => {
                 // Create data object mimicking history entry structure for consistency
                 const imageDataForHistory = {
                     url: image.url,
                     prompt: prompt,
                     seed: seed,
                     has_nsfw_concepts: has_nsfw_concepts,
                     // Include other details if needed (e.g., timings, model used?)
                     // timings: data.timings ? JSON.stringify(data.timings) : '[Timings missing]',
                 };
                console.log('Prepending new image to history DOM:', imageDataForHistory);
                appendImageToHistoryDOM(imageDataForHistory, true); // Prepend new image
            });

        } catch (error) {
             console.error("Error inside handleFinalStream:", error, "Original data:", data);
             appendLog("Error processing final result data. See console.");
        }
    }

    // Function to add a single image element to the history grid
    function appendImageToHistoryDOM(entryData, prepend = false) {
        const historyContainer = document.getElementById('history');
        const historyEntry = document.createElement('div');
        historyEntry.className = `history-entry ${currentSizeClass}`; // Apply current size class
        historyEntry.id = `history-item-${entryData.request_id}`;

        const img = document.createElement('img'); 
        img.src = entryData.url;
        img.alt = 'Generated Image';
        img.className = 'thumbnail';
        img.loading = 'lazy';
        
        if (entryData.request_id) {
            const deleteBtn = document.createElement('button');
            deleteBtn.className = 'history-delete-btn';
            deleteBtn.innerHTML = '&times;'; 
            deleteBtn.title = 'Delete this result';
            deleteBtn.addEventListener('click', (e) => {
                e.stopPropagation(); 
                handleDeleteHistoryItem(entryData.request_id, historyEntry);
            });
            historyEntry.appendChild(deleteBtn);
        } else {
            console.warn("Cannot add delete button, request_id missing for:", entryData.url);
        }
        
        historyEntry.appendChild(img); // Add image AFTER button

        // Prepare modal data object
        const imageObjForModal = {
             url: entryData.url,
             prompt: entryData.prompt || '',
             seed: entryData.seed || '',
             has_nsfw_concepts: entryData.has_nsfw_concepts !== undefined ? entryData.has_nsfw_concepts : '',
             timings: entryData.timings || '',
             request_id: entryData.request_id
         };

        // --- Masonry Integration --- 
        if (prepend) {
            historyContainer.insertBefore(historyEntry, historyContainer.firstChild);
            allHistoryImages.unshift(imageObjForModal);
        } else {
            historyContainer.appendChild(historyEntry);
            allHistoryImages.push(imageObjForModal);
        }

        if (msnry) {
            imagesLoaded(historyEntry, function() {
                console.log(`Image loaded for ${entryData.request_id || 'new item'}, adding to Masonry.`);
                if (prepend) {
                    msnry.prepended(historyEntry); 
                } else {
                    msnry.appended(historyEntry);  
                }
                 // Optional layout trigger 
                 // msnry.layout(); 
            });
        } else {
            console.warn("Masonry not initialized yet when trying to add item:", entryData.request_id);
        }
         // --- End Masonry Integration --- 

         img.addEventListener('click', () => { 
            currentHistoryImageIndex = allHistoryImages.findIndex(item => item.request_id === entryData.request_id);
            if (currentHistoryImageIndex === -1) currentHistoryImageIndex = 0; 
            showImage(currentHistoryImageIndex);
            document.getElementById('modalPrompt').textContent = `Prompt: ${entryData.prompt || 'N/A'}`;
            document.getElementById('modalSeed').textContent = `Seed: ${entryData.seed || 'N/A'}`;
            document.getElementById('modalNSFW').textContent = `NSFW Concepts: ${entryData.has_nsfw_concepts !== undefined ? entryData.has_nsfw_concepts : 'N/A'}`;
            document.getElementById('modalTimings').textContent = `Timings: ${entryData.timings || 'N/A'}`; 
            document.getElementById('imageModal').classList.add('modal-open');
         });
    }

    // Function to display history - Now fetches from API and supports pagination
    async function displayHistory(endpoint = null, page = 1) {
        // If the endpoint changes, reset pagination
        if (endpoint && endpoint !== currentHistoryEndpoint) {
            console.log(`History endpoint changed from ${currentHistoryEndpoint} to ${endpoint}. Resetting page.`);
            currentHistoryPage = 1;
            hasMoreHistory = true;
            currentHistoryEndpoint = endpoint;
            // Clear previous history immediately for responsiveness
            const historyContainer = document.getElementById('history');
            historyContainer.innerHTML = ''; 
            allHistoryImages = []; 
        } else if (!endpoint && !currentHistoryEndpoint) {
            // Use the first available model if neither current nor new endpoint is set
            currentHistoryEndpoint = modelSelect.value || (modelSelect.options[1] ? modelSelect.options[1].value : null);
            console.log(`Initial history endpoint set to: ${currentHistoryEndpoint}`);
        }

        const modelToFetch = currentHistoryEndpoint;

        // Only proceed if we have a valid, non-empty model endpoint identifier
        if (!modelToFetch || modelToFetch === "") { 
            console.warn("displayHistory: No valid model endpoint selected. Skipping history fetch.");
            const historyContainer = document.getElementById('history');
            historyContainer.innerHTML = '<p class="text-center col-span-4">Select a model to view its history.</p>';
            isLoadingHistory = false; // Ensure loading state is reset
            hasMoreHistory = false; // No history to load
            return;
        }

        // Prevent loading if already loading or no more pages
        if (isLoadingHistory || (page > 1 && !hasMoreHistory)) {
            console.log(`Skipping history load. isLoading: ${isLoadingHistory}, page: ${page}, hasMore: ${hasMoreHistory}`);
            return;
        }

        isLoadingHistory = true;
        const historyContainer = document.getElementById('history');
        let newElements = []; // Keep track of new elements added in this batch

        if (page === 1) {
            // Check if msnry variable exists and is not null before destroying
            if (typeof msnry !== 'undefined' && msnry) { 
                 console.log("Destroying Masonry for page 1 load.");
                 msnry.destroy();
                 msnry = null;
            } else {
                 console.log("Masonry instance not found or not defined, skipping destroy.");
            }
            historyContainer.innerHTML = '';
            allHistoryImages = [];
            hasMoreHistory = true;
        }
        
        const loadingIndicatorId = `loading-page-${page}`;
        if (page > 1) {
             const loadingDiv = document.createElement('div');
             loadingDiv.id = loadingIndicatorId;
             loadingDiv.className = 'text-center col-span-4 p-4'; 
             loadingDiv.innerHTML = '<span class="loading loading-dots loading-lg"></span>';
             // Append to body temporarily? Or a dedicated loading area?
             // Appending directly to history container might interfere with masonry
             document.body.appendChild(loadingDiv); // Append to body
        }

        console.log(`Fetching history for endpoint: ${modelToFetch}, page: ${page}`); // modelToFetch needs defining earlier
        try {
            const modelToFetch = currentHistoryEndpoint; // Get the current endpoint
            if (!modelToFetch) throw new Error("No history endpoint selected"); // Basic check
            
            const historyUrl = `/api/history?endpoint=${encodeURIComponent(modelToFetch)}&page=${page}`;
            const response = await fetch(historyUrl);
            
            if (!response.ok) {
                throw new Error(`Failed to fetch history: ${response.status} ${response.statusText}`);
            }
            
            const historyData = await response.json();
            console.log("Received history data:", historyData);

            const loadingDiv = document.getElementById(loadingIndicatorId);
            if (loadingDiv) loadingDiv.remove();

            if (historyData && Array.isArray(historyData.items)) {
                if (historyData.items.length === 0) {
                    console.log("No more history items found.");
                    hasMoreHistory = false; 
                    if (page === 1) { 
                         historyContainer.innerHTML = '<p class="text-center col-span-4">No history found for this model.</p>';
                    }
                } else {
                    
                    historyData.items.forEach(item => {
                        
                        if (item.json_output && Array.isArray(item.json_output.images) && item.json_output.images.length > 0) {
                             const firstImage = item.json_output.images[0]; 
                             const entryData = {
                                 url: firstImage.url,
                                 prompt: item.json_output.prompt || (item.json_input ? item.json_input.prompt : '[Prompt N/A]'), 
                                 seed: item.json_output.seed !== undefined ? item.json_output.seed : '[Seed N/A]',
                                 has_nsfw_concepts: Array.isArray(item.json_output.has_nsfw_concepts) ? item.json_output.has_nsfw_concepts[0] : '[NSFW N/A]',
                                 request_id: item.request_id || null 
                             };

                            const historyEntry = document.createElement('div');
                            historyEntry.className = `history-entry ${currentSizeClass}`; 
                            historyEntry.id = `history-item-${entryData.request_id}`;

                            const img = document.createElement('img');
                            img.src = entryData.url;
                            img.alt = 'Generated Image';
                            img.className = 'thumbnail';
                            img.loading = 'lazy';

                            if (entryData.request_id) {
                                 const deleteBtn = document.createElement('button');
                                 deleteBtn.className = 'history-delete-btn';
                                 deleteBtn.innerHTML = '&times;';
                                 deleteBtn.title = 'Delete this result';
                                 deleteBtn.addEventListener('click', (e) => {
                                     e.stopPropagation();
                                     handleDeleteHistoryItem(entryData.request_id, historyEntry);
                                 });
                                 historyEntry.appendChild(deleteBtn);
                             } else {
                                console.warn("Cannot add delete button, request_id missing for:", entryData.url);
                             }

                            historyEntry.appendChild(img); 
                             
                             img.addEventListener('click', () => { 
                                  currentHistoryImageIndex = allHistoryImages.findIndex(item => item.request_id === entryData.request_id);
                                  if (currentHistoryImageIndex === -1) currentHistoryImageIndex = 0; 
                                  showImage(currentHistoryImageIndex);
                                  document.getElementById('modalPrompt').textContent = `Prompt: ${entryData.prompt || 'N/A'}`;
                                  document.getElementById('modalSeed').textContent = `Seed: ${entryData.seed || 'N/A'}`;
                                  document.getElementById('modalNSFW').textContent = `NSFW Concepts: ${entryData.has_nsfw_concepts !== undefined ? entryData.has_nsfw_concepts : 'N/A'}`;
                                  document.getElementById('modalTimings').textContent = `Timings: ${entryData.timings || 'N/A'}`; 
                                  document.getElementById('imageModal').classList.add('modal-open');
                             });
                             
                             newElements.push(historyEntry); 
                             allHistoryImages.push({
                                 url: entryData.url,
                                 prompt: entryData.prompt,
                                 seed: entryData.seed,
                                 has_nsfw_concepts: entryData.has_nsfw_concepts,
                                 request_id: entryData.request_id
                             }); 
                        } else {
                            console.warn("History item skipped - missing json_output or json_output.images:", item);
                        }
                    });

                    // Append all new elements to the container
                    newElements.forEach(el => historyContainer.appendChild(el));

                    // --- Initialize or Add to Masonry ---
                    if (page === 1) {
                        initMasonry(); // Initialize Masonry after first page elements added
                    } else if (msnry && newElements.length > 0) {
                         imagesLoaded(newElements, function() {
                             console.log(`Images loaded for page ${page}, adding ${newElements.length} items to Masonry.`);
                             msnry.appended(newElements); // Add new items to layout
                             // Optional: msnry.layout(); 
                         });
                    }
                     // --- End Masonry ---

                    currentHistoryPage = page;
                    hasMoreHistory = true;
                }
            } else {
                 console.error("Unexpected history data structure:", historyData);
                 hasMoreHistory = false; 
                 if (page === 1) {
                      historyContainer.innerHTML = '<p class="text-center col-span-4">Error parsing history data.</p>';
                 }
            }

        } catch (error) {
            console.error("Error fetching or displaying history:", error);
            hasMoreHistory = false; 
            const loadingDiv = document.getElementById(loadingIndicatorId);
            if (loadingDiv) loadingDiv.remove();
            if (page === 1) {
                 historyContainer.innerHTML = `<p class="text-center col-span-4 text-error">Error loading history. Check console.</p>`;
            } else {
                historyContainer.innerHTML += `<p class="text-center col-span-4 text-error">Error loading more history.</p>`;
            }
        } finally {
             isLoadingHistory = false;
             console.log(`History load finished. isLoading: ${isLoadingHistory}, hasMore: ${hasMoreHistory}, currentPage: ${currentHistoryPage}`);
        }
    }

    // Function to show image in modal - Now uses allHistoryImages array
    function showImage(index) {
        if (allHistoryImages.length > 0) {
            currentHistoryImageIndex = (index + allHistoryImages.length) % allHistoryImages.length;
            const imageObj = allHistoryImages[currentHistoryImageIndex];
            if (!imageObj) {
                console.error("showImage: Invalid image object at index", currentHistoryImageIndex);
                return;
            }
            document.getElementById('modalImage').src = imageObj.url;
            document.getElementById('modalPrompt').textContent = `Prompt: ${imageObj.prompt || 'N/A'}`;
            document.getElementById('modalSeed').textContent = `Seed: ${imageObj.seed || 'N/A'}`;
            document.getElementById('modalNSFW').textContent = `NSFW Concepts: ${imageObj.has_nsfw_concepts !== undefined ? imageObj.has_nsfw_concepts : 'N/A'}`;
            document.getElementById('modalTimings').textContent = `Timings: ${imageObj.timings || 'N/A'}`; // Display timings if available
        } else {
             console.warn("showImage called but allHistoryImages is empty.");
        }
    }

    function showPreviousImage() {
        showImage(currentHistoryImageIndex - 1);
    }

    function showNextImage() {
        showImage(currentHistoryImageIndex + 1);
    }

    document.addEventListener('keydown', (event) => {
        if (event.key === 'ArrowLeft') {
            showPreviousImage();
        } else if (event.key === 'ArrowRight') {
            showNextImage();
        } else if (event.key === 'Escape') {
            document.getElementById('imageModal').classList.remove('modal-open');
        }
    });

    // Add event listeners for the new buttons
    document.getElementById('downloadBtn').addEventListener('click', () => {
        const imageObj = allHistoryImages[currentHistoryImageIndex];
        downloadImage(imageObj.url, `generated-image-${currentHistoryImageIndex + 1}.png`);
    });

    document.getElementById('reusePromptBtn').addEventListener('click', () => {
        const imageObj = allHistoryImages[currentHistoryImageIndex];
        document.getElementById('prompt').value = imageObj.prompt; // Update the textarea with ID 'prompt'
        document.getElementById('imageModal').classList.remove('modal-open');
    });

    document.getElementById('closeModalBtn').addEventListener('click', () => {
        document.getElementById('imageModal').classList.remove('modal-open');
    });

    // --- Add listener to close modal on backdrop click --- 
    const imageModal = document.getElementById('imageModal');
    if (imageModal) {
        imageModal.addEventListener('click', (event) => {
            // Check if the direct click target is the modal backdrop itself
            if (event.target === imageModal) { 
                imageModal.classList.remove('modal-open');
                console.log("Modal closed by clicking backdrop."); 
            }
        });
    }    
    // --- End backdrop click listener --- 

    // Call displayHistory on page load - moved inside loadModels to ensure modelSelect is populated
    // displayHistory();

    async function loadModels() {
        try {
            // Remove potential fetching logic if present
            // const response = await fetch('models.json'); 
            // const models = await response.json();

            // Explicitly use the global MODELS array
            const models = MODELS; 
            
            const select = document.getElementById('modelSelect');
            if (!select) {
                console.error("Fatal: modelSelect element not found in the DOM.");
                return;
            }

            select.innerHTML = '<option value="">Select an endpoint</option>';
            models.forEach(model => {
                const option = document.createElement('option');
                option.value = model.model_path;
                option.textContent = model.name;
                select.appendChild(option);
            });

            let initialModelEndpoint = ""; 
            if (select.options.length > 1) { 
                select.selectedIndex = 1; 
                initialModelEndpoint = select.value; 
                console.log(`Initial model selected: ${initialModelEndpoint}`);
            } else {
                 console.log("No models loaded, cannot set initial selection.");
            }
             
            // --- Reset history state on model change --- 
             select.addEventListener('change', () => { 
                const selectedValue = select.value;
                console.log(`#modelSelect change event fired. Value: "${selectedValue}"`); 
                // No need to reset vars here, displayHistory handles it
                displayHistory(selectedValue, 1); // Explicitly call page 1 for the new model
             });
             // --- End Reset --- 

            console.log('loadModels finished, calling updateDropdownVisibility and initial displayHistory.');
            updateDropdownVisibility(); 
            
            console.log(`Calling initial displayHistory with endpoint: "${initialModelEndpoint}"`);

            displayHistory(initialModelEndpoint, 1); // Explicitly call page 1

        } catch (error) {
            console.error('Error loading models:', error);
        }
    }
    
    loadModels();
    
    function submitModel() {
        const selectedModel = document.getElementById('modelSelect').value;
        if (selectedModel) {
            fetch('/submit', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ model: selectedModel })
            }).then(response => {
                if (response.ok) {
                    alert('Model submitted successfully');
                } else {
                    alert('Failed to submit model');
                }
            });
        } else {
            alert('Please select a model');
        }
    }

    // Add Scroll Event Listener for Infinite Scroll
    window.addEventListener('scroll', () => {
        // Check if user is near the bottom
        // Use document.documentElement for broader compatibility
        const scrollHeight = document.documentElement.scrollHeight;
        const scrollTop = document.documentElement.scrollTop || document.body.scrollTop; // Handle cross-browser
        const clientHeight = document.documentElement.clientHeight;
        const threshold = 300; // Pixels from bottom to trigger load

        if (scrollHeight - scrollTop <= clientHeight + threshold) {
            // Check if not already loading and if more pages exist
            if (!isLoadingHistory && hasMoreHistory) {
                console.log("Near bottom, attempting to load next history page...");
                displayHistory(currentHistoryEndpoint, currentHistoryPage + 1);
            }
        }
    });

    // New function to handle deletion
    async function handleDeleteHistoryItem(requestId, elementToRemove) {
        console.log(`Attempting to delete history item with request ID: ${requestId}`);
        if (!confirm(`Are you sure you want to delete this history item?\nRequest ID: ${requestId}\nThis will attempt to remove the image data from Fal storage.`)) {
            return;
        }

        try {
            elementToRemove.style.opacity = '0.5'; // Visual feedback: Dim the item
            const response = await fetch(`/api/delete-fal-request/${requestId}`, {
                method: 'POST', // Use POST as it's performing an action
            });

            if (response.ok) {
                const result = await response.json();
                console.log('Deletion successful:', result);

                // --- Masonry Integration --- 
                if (typeof msnry !== 'undefined' && msnry) {
                     console.log(`Removing element ${requestId} from Masonry.`);
                     msnry.remove(elementToRemove); // Tell Masonry to remove the element
                     console.log(`Triggering Masonry layout after removal.`);
                     msnry.layout(); // Re-calculate layout
                } else {
                     console.warn("Masonry instance not found, removing element directly from DOM.");
                     elementToRemove.remove(); // Fallback: Remove directly if masonry isn't ready
                }
                 // --- End Masonry Integration ---
                 
                 // Note: elementToRemove.remove() might be redundant now, but keep for safety
                 if (elementToRemove.parentNode) { // Check if it wasn't already removed by masonry.layout()
                    elementToRemove.remove();
                 }

                // Remove from the modal array
                const indexToRemove = allHistoryImages.findIndex(item => item.request_id === requestId);
                if (indexToRemove > -1) {
                    allHistoryImages.splice(indexToRemove, 1);
                    console.log(`Removed item ${requestId} from modal array.`);
                }

            } else {
                const errorResult = await response.json();
                console.error(`Failed to delete item ${requestId}:`, response.status, errorResult);
                alert(`Error deleting item: ${errorResult.error || response.statusText}`);
                elementToRemove.style.opacity = '1'; // Restore opacity on failure
            }
        } catch (error) {
            console.error(`Network or other error deleting item ${requestId}:`, error);
            alert(`Error deleting item: ${error.message}`);
            elementToRemove.style.opacity = '1'; // Restore opacity on failure
        }
    }

    if (strengthSlider && strengthInput && strengthValue) {
        strengthSlider.addEventListener('input', function() {
            strengthInput.value = this.value;
            strengthValue.textContent = this.value;
        });
        strengthInput.addEventListener('change', function() { // Use 'change' for number input for better UX
            let value = parseFloat(this.value);
            if (value < 0.01) value = 0.01;
            if (value > 1.0) value = 1.0;
            this.value = value; // Update input if adjusted
            strengthSlider.value = value;
            strengthValue.textContent = value;
        });
         // Also handle direct input changes
         strengthInput.addEventListener('input', function() {
             let value = parseFloat(this.value);
              // Don't clamp immediately on input, wait for change/blur
              strengthValue.textContent = this.value; // Update label in real-time
              // Optionally update slider in real-time too, but can be jerky
              // if (!isNaN(value) && value >= 0.01 && value <= 1.0) {
              //    strengthSlider.value = value;
              // }
         });

    } else {
        console.warn("Strength slider/input/value elements not found.");
    }

    // --- Image Preview Logic ---
    if (imageUploadInput && imagePreview) {
        imageUploadInput.addEventListener('change', async function(event) {
            const file = event.target.files[0];
            if (file && file.type.startsWith('image/')) {
                // Show preview of the image
                const reader = new FileReader();
                reader.onload = function(e) {
                    imagePreview.src = e.target.result;
                    imagePreview.classList.remove('hidden'); // Show preview
                }
                reader.readAsDataURL(file);
                
                // Automatically upload the file to UploadThing
                try {
                    const uploadedUrl = await uploadFileToUploadThing(file);
                    console.log("File uploaded successfully, URL:", uploadedUrl);
                    
                    // Store the URL for later use during form submission
                    const uploadedImageUrl = document.getElementById('uploadedImageUrl');
                    if (uploadedImageUrl) {
                        uploadedImageUrl.value = uploadedUrl;
                    }
                } catch (error) {
                    console.error("Error during automatic upload:", error);
                    alert(`Failed to upload image: ${error.message}`);
                }
            } else {
                imagePreview.src = '#'; // Clear preview
                imagePreview.classList.add('hidden'); // Hide preview
                // Clear the hidden input
                const uploadedImageUrl = document.getElementById('uploadedImageUrl');
                if (uploadedImageUrl) {
                    uploadedImageUrl.value = '';
                }
                
                if (file) { // If a file was selected but wasn't an image
                    alert("Please select a valid image file.");
                    imageUploadInput.value = ''; // Clear the input
                }
            }
        });
    } else {
        console.warn("Image upload input or preview element not found.");
    }
    // --- End Image Preview ---

    // Helper function to read file as Data URL
    function readFileAsDataURL(file) {
        return new Promise((resolve, reject) => {
            const reader = new FileReader();
            reader.onload = () => resolve(reader.result);
            reader.onerror = (error) => reject(error);
            reader.readAsDataURL(file);
        });
    }

    loadModels(); // Load models when DOM is ready
});

// Function to handle clipboard paste
function setupClipboardPaste() {
    console.log('Setting up clipboard paste functionality');
    
    // Add event listeners for paste events
    document.addEventListener('paste', handlePaste);
    
    // Make pasteTarget elements clickable and give focus for paste
    const pasteTarget = document.getElementById('pasteTarget');
    if (pasteTarget) {
        pasteTarget.addEventListener('click', () => {
            pasteTarget.classList.add('pulse'); // Add pulse animation when clicked
            
            // Show a tooltip or message
            const pasteIndicator = pasteTarget.querySelector('span');
            if (pasteIndicator) {
                pasteIndicator.textContent = 'Press Ctrl+V to paste image';
            }
            
            // Set focus to enable paste events
            pasteTarget.focus();
            
            // Remove pulse class after animation completes
            setTimeout(() => {
                pasteTarget.classList.remove('pulse');
                
                // Reset tooltip
                if (pasteIndicator) {
                    pasteIndicator.textContent = 'Drop image here or click to paste from clipboard';
                }
            }, 1500);
        });
    }
    
    // Also make the container itself paste-enabled
    const imageUploadContainer = document.getElementById('imageUploadContainer');
    if (imageUploadContainer) {
        imageUploadContainer.classList.add('paste-enabled');
        imageUploadContainer.tabIndex = 0; // Make focusable
    }
}

function handlePaste(event) {
    console.log('Paste event detected');
    
    // Check if we're focused in a text input
    const activeElement = document.activeElement;
    const isTextInput = activeElement.tagName === 'INPUT' || 
                       activeElement.tagName === 'TEXTAREA' ||
                       activeElement.isContentEditable;
    
    // If we're in a text input and it's not the paste target, let the default paste happen
    if (isTextInput && activeElement.id !== 'pasteTarget') {
        console.log('Paste event in text input, ignoring for image paste');
        return;
    }
    
    // Look for image data in clipboard
    const items = (event.clipboardData || event.originalEvent.clipboardData).items;
    
    for (const item of items) {
        if (item.type.indexOf('image') === 0) {
            console.log('Image found in clipboard');
            
            // Prevent default paste
            event.preventDefault();
            
            // Get the blob/file from clipboard
            const blob = item.getAsFile();
            
            // Process the file
            processImageFile(blob);
            return;
        }
    }
    
    console.log('No image found in clipboard');
}

function processImageFile(file) {
    if (!file) {
        console.error('No file provided to processImageFile');
        return;
    }
    
    console.log('Processing pasted image file:', file.name || 'unnamed', file.type, file.size);
    
    // Show success indicator
    const pasteTarget = document.getElementById('pasteTarget');
    if (pasteTarget) {
        pasteTarget.innerHTML = '<div class="paste-success">Image received! Processing...</div>';
    }
    
    // Create a preview of the image
    const imagePreview = document.getElementById('imagePreview');
    if (imagePreview) {
        // Read the file as data URL
        readFileAsDataURL(file).then(dataUrl => {
            // Display the image
            imagePreview.src = dataUrl;
            imagePreview.classList.remove('hidden');
            
            // Upload the file
            uploadFileToUploadThing(file).then(fileUrl => {
                console.log('Upload complete, file URL:', fileUrl);
                
                // Update paste target with success message
                if (pasteTarget) {
                    pasteTarget.innerHTML = '<div class="paste-success">Image uploaded successfully!</div>';
                }
                
                // Add a short delay then reset the paste target
                setTimeout(() => {
                    if (pasteTarget) {
                        pasteTarget.innerHTML = '<span class="text-sm">Drop image here or click to paste from clipboard</span>';
                    }
                }, 3000);
            }).catch(error => {
                console.error('Error uploading pasted image:', error);
                
                // Show error in paste target
                if (pasteTarget) {
                    pasteTarget.innerHTML = `<div class="paste-error">Upload failed: ${error.message}</div>`;
                }
            });
        }).catch(error => {
            console.error('Error reading pasted image file:', error);
        });
    }
}

function readFileAsDataURL(file) {
    return new Promise((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = () => resolve(reader.result);
        reader.onerror = () => reject(new Error('Failed to read file'));
        reader.readAsDataURL(file);
    });
}
